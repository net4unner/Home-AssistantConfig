- alias: "Bedtime"
  trigger:
    - platform: time
      after: "22:00:00"
  condition:
    condition: and
    conditions:
      - condition: time
        weekday:
          - mon
          - tue
          - wed
          - thu
          - sun
      - condition: time
        before: "22:30:00"
  action:
    - service: light.turn_off
      entity_id: group.all_lights
    - service: alarm_control_panel.alarm_arm_home
